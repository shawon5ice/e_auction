import 'package:flutter/material.dart';

class AuctionGalleryScreen extends StatelessWidget {
  const AuctionGalleryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
